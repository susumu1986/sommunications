# Sommunications

日本語でコミュニケーション能力を磨くアプリのランディングページです。

## ローカルでの起動方法

このプロジェクトは静的HTML/CSSのみで構成されています。以下の手順でローカル環境から確認できます。

1. ルートディレクトリで簡易サーバーを起動します。

   ```bash
   python server.py
   ```

2. ブラウザで以下にアクセスします。

   ```text
   http://localhost:8000/
   ```

停止するときはターミナルで `Ctrl + C` を押してください。
